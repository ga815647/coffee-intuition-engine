__pycache__/
*.py[cod]
.venv/
venv/
.env
secrets/
*.egg-info/
.pytest_cache/
.qdrant/
.wrangler/
*.log

# 可重生衍生物 / 運行期快取(不入庫)。真相已入庫:corpus/global.jsonl(curated)
# + corpus/raw/*.jsonl(provenance);產生工具見 tools/。下列皆可重建或為遠端副本:
#   - 向量索引:本地 .qdrant/(見上方);Vectorize 為遠端、無本地檔。
#   - canonical 本地快取:R2 為遠端真相,本地 ./data/canonical.jsonl 僅運行期副本。
#   - 評測報告:python -m eval.run 重生。
data/canonical.jsonl
data/canonical/
eval/report.json
# 全量 D1 備份(python -m cie.export):含 self 個資(各 guest 命名空間),**絕不入公開 git**。
# 只 global 的快照(可公開、誤刪可復原)走 cie.snapshot → corpus/global.export.jsonl(該檔入庫)。
backups/
