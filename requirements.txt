# Core
pydantic>=2.6
numpy>=1.26
qdrant-client>=1.9

# MCP server(stdio + remote streamable-http)
mcp>=1.2
# remote MCP HTTP 邊緣(server_http.py);starlette/httpx 多由 mcp 帶入,顯式釘住以防部署精簡。
uvicorn>=0.30
starlette>=0.37
httpx>=0.27

# Cloudflare 後端(Workers AI 嵌入 + Vectorize 向量庫)走 stdlib urllib,零新依賴。
# 選配雲端嵌入供應商(需要才解註):
# openai>=1.30
# voyageai>=0.2

# Dev
pytest>=8.0
